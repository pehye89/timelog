# TimeLog

업무 시간을 타이머로 기록하고, 운영 단위로 관리하고, 주간 보고서를 만드는 개인용 웹 앱입니다.
빌드 과정 없는 정적 사이트라 GitHub Pages 등 정적 호스팅에 폴더 그대로 올리면 바로 동작합니다.

## 구조

```
index.html          진입점, 모든 탭/모달 마크업
css/style.css        전체 스타일 (디자인 토큰 기반)
js/
  utils.js           공용 헬퍼, 불릿 에디터
  state.js            전역 상태
  storage.js          localStorage 입출력
  settings.js          설정 모달 로직
  backup.js            Google Drive 온라인 백업
  tracker.js            타이머 탭
  history.js           타임로그 목록/수정
  timeline.js          그리드 타임라인
  management.js         운영 관리 탭
  weekly.js              주간보고 탭
  modals.js               모달 공통 열기/닫기
  app.js                   초기화, 이벤트 바인딩
```

데이터는 브라우저 `localStorage`에 저장됩니다 (서버 없음). 다른 기기와 동기화하려면 설정 상단의
"내보내기 · 백업" 모달에서 파일 내보내기/가져오기 또는 Google Drive 온라인 백업을 사용하세요.

## 버전 관리

버전은 `yymmdd.nn` 형식입니다 (예: `260827.01` = 2026년 8월 27일의 첫 번째 배포).
- 앱 안에서: 설정(⚙️) 모달 맨 아래 중앙에 옅게 표시됩니다.
- 코드 캐시 무효화: `index.html`의 모든 `css/js` 링크에 `?v=yymmdd.nn` 쿼리스트링으로 붙어 있어,
  배포할 때마다 브라우저가 옛 캐시 대신 새 파일을 받아오도록 강제합니다.

**이 저장소를 수정할 때마다, 아래 변경 이력에 새 버전 항목을 추가합니다.** 같은 날 여러 번 수정하면
`.02`, `.03`처럼 일련번호만 올라가고, 날짜가 바뀌면 `.01`부터 다시 시작합니다.

## 변경 이력 (Changelog)

### 260828.01
- 새 날짜로 버전 번호 갱신 (기능 변경 없음)

### 260827.06
- 주간보고 탭에서 진행상태(진행중/완료)를 더 이상 수정할 수 없도록 변경 — 클릭 가능한 버튼 대신 읽기 전용 배지로 표시 (수정은 운영 관리 탭에서만 가능)
- 주간보고 탭 날짜 이동의 "오늘" 버튼 워딩을 "금주"로 변경 (주 단위 탐색이라는 맥락에 더 맞게)

### 260827.05
- 타임로그 카드의 운영정보(제목/메타)와 아래 상세 목록(타임로그 트리) 사이 간격 확대
- 관리업무는 시작일/완료일 행을 아예 표시하지 않도록 변경 (기존엔 `-`/`-`로 표시했었음)
- 진행상태(진행중/완료) 드롭다운이 스크롤된 상태에서 버튼 바로 아래가 아닌 엉뚱한 위치에 뜨던 버그 수정
  - 원인: 드롭다운이 `position: fixed`(뷰포트 기준 좌표)인데 위치 계산 시 `window.scrollY`/`scrollX`(페이지 스크롤 오프셋)를 추가로 더해서, 스크롤된 만큼 이중으로 밀려나 있었음
  - `getBoundingClientRect()` 값을 스크롤 보정 없이 그대로 사용하도록 수정

### 260827.04
- 운영명과 상세정보(운영번호/작업명, 시작일/완료일) 사이 간격을 더 좁게(밀착) 조정
- 진행중 ↔ 완료 전환에 드롭다운 + 토스트 알림을 다시 추가 (카테고리 없이 단순 두 상태만: 진행중 버튼 → "완료" 선택지, 완료 버튼 → "진행중으로 변경" 선택지)
- 버전 표시를 다시 footer 안에 한 줄로 (`ver 260827.04`)

### 260827.03
- **260827.02로 롤백**: "완료 카테고리(하위 상태)" 관련 기능 전체 제거 (검색창 옆 드롭다운, 완료/진행중 빠른 선택 메뉴, 완료 시 토스트 알림, footer 버전 병합). 설정 데이터 모델·수정 모달·목록 렌더링을 롤백 이전 상태로 복원하고, 버전 표시도 다시 설정 모달 안으로 이동
- 타이머 탭 타임로그 카드에 해당 운영의 **시작일/완료일**을 표시 (운영 관리·주간보고 탭과 동일한 형식). 관리업무는 기존과 동일하게 `-`/`-`로 표시

### 260827.02
- 이 README에 변경 이력 관리 방식 도입 (앞으로 모든 수정은 여기에 기록)

### 260827.01
- 설정 모달 하단 중앙에 현재 버전(`yymmdd.nn`)을 옅게 표시하는 기능 추가
